name: souluniverse_coding_test
description: 소울유니버스 개발자 채용 코딩 테스트 — 레거시 코드 디버깅
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  cloud_firestore: ^6.0.3
  dio: ^5.4.3
  go_router: ^14.2.0
  intl: ^0.19.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
